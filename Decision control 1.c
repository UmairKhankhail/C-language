main()
{
    int x;
    printf("Enter any number: ");
    scanf("%d",&x);
    if (x>0)
        printf("Positive number");
    else
    {
        printf("Non positive number");
    }
    getch();
}

// Now this program contains one declaration and five action statements
