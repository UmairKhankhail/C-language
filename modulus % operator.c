// used to check divisibility
// used to find the last,second last digit when divided by 10, 100 respectively

//  the sign of numerator(digit on the left side of modulus) is always the sign of remainder

main()
{
    int a=345%55;
    printf("%d",a);
    getch();

}

